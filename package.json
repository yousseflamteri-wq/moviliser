{
  "name": "your-app-name",
  "private": true,
  "version": "0.0.0",
  "type": "module", 
  "scripts": {
    "content": "node scripts/build-content.js",
    "dev": "npm run content && vite",
    "build": "npm run content && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    // ... your dependencies remain untouched
  }
}
